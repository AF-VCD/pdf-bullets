name: Push to GitLab CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: prep and push mirror
        run: |
          git config user.password ${{ secrets.GITLAB_PUSH_KEY }}
          git config user.name project_2367_bot
          git remote add mirror 'https://code.il2.dsop.io/chkodama/pdf-bullets.git'
          git push mirror
          
          
          
          
          
